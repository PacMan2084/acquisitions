
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/acquisitions
ARCJET_KEY=ajkey_01kcz0h6mhfz6rerq5vr20q187
NODE_ENV=development
PORT=3000


# Development configuration (Docker + local PostgreSQL)

# The app connects to the `postgres` container from `docker-compose.dev.yml`.
# `DATABASE_URL` above should point at that container (see line 2).
# Neon Local is no longer used for development; Neon Cloud is only used in
# production via `.env.production`.

# Arcjet
#ARCJET_KEY=ajkey_01kcz0h6mhfz6rerq5vr20q187
